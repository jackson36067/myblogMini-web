export type sortItemResult = { name: string; sort: number };

export type userDataResult = {
  id: string;
  avatar: string;
  nickName: string;
  comment: string;
  isFollow: boolean;
};
export type moreUserData = {
  id: string;
  nickName: string;
  comment: string;
};
